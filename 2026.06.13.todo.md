# TODO — 2026-06-13

> Cerrar el hueco de persistencia: el pipeline MQTT corre pero **no guarda nada**.
> Mantenerlo **simple**: persistir el turno crudo (niño → bot), sin IA extra.
> El análisis/inferencia (Gemini clasificando) es P2, NO hoy.

## Contexto

- Rama de trabajo: `refactor/foundations` (10 commits sobre `main`, sin PR).
- `internal/core/orchestrator.go` NO referencia el store → nada se persiste.
- Ya existe todo lo necesario en `internal/store`:
  - `CreateInteraction(...)` — escribe la fila
  - `GetActiveChildForDevice(deviceID)` — resuelve device → child activo
  - `TouchDeviceLastSeen(id)` — bonus, marca last_seen

## Decisión de diseño (resolver antes de codear)

`HandleAudio` hoy devuelve **solo audio**, y no conoce el `device_id` (vive en el
topic MQTT). Dos opciones:

- **(A) recomendada** — `HandleAudio` devuelve un struct rico
  (`transcript`, `responseText`, latencias). La **capa MQTT** (que sí tiene el
  `device_id` del topic) resuelve el child y llama `CreateInteraction`.
  → orquestador queda puro, persistencia y atribución viven en el ingest.
- (B) inyectar `store` + `device_id` dentro del orquestador y persistir ahí.
  → más acoplado, mezcla pipeline con DB.

Ir con **(A)** salvo que surja algo.

## Tareas de hoy — HECHO ✅

- [x] Cronometrar cada etapa en `HandleAudio` (STT / LLM / TTS) con `time.Now()`.
- [x] Cambiar `HandleAudio` para devolver `*core.TurnResult`
      (`transcript`, `responseText`, `stt/llm/tts/total latency ms`) + audio.
      También `LocalOrchestrator` y `local_recorder` adaptados.
- [x] En la capa MQTT (`internal/mqtt/client.go`): `processSession` →
      - resuelve `child_id` con `GetActiveChildForDevice(uuid del topic)`,
      - genera `session_id` (uuid) por sesión,
      - llama `CreateInteraction(...)` con el turno + latencias.
- [x] Persistencia **no fatal** (`persistInteraction`): si falla la DB se loguea
      y NO rompe la respuesta de voz al niño.
- [x] `child_id` nulo permitido (device sin asignar) → guarda igual.
- [x] `store.Querier` nil-safe: `cmd/mqtt` solo crea pool si `DATABASE_URL` está
      seteado; sin DB el ingest corre igual sin persistir. (`config.DatabaseURL`).
- [x] `go build ./...` + `go vet ./...` limpio.
- [x] `go test -race ./...` pasa (+ test nuevo `TestOrchestrator_HandleAudio`).

## Verificación real — HECHO ✅ (2026-06-14)

> Smoke test end-to-end contra Postgres real, todo verde.

- [x] `docker compose up` (postgres:5433 + mosquitto:1883), migraciones goose (v2).
- [x] Seed: account `a111…`/parent@test.com, child `c111…`/Mateo,
      device `d111…`/SN-TEST-001 + assignment activo.
- [x] `cmd/mqtt` con `DATABASE_URL` + `cmd/devicesim -device d111…` → round-trip OK.
- [x] Fila en `interactions` con `child_id` resuelto a `c111…` (latencias incluidas).
- [x] Worker de análisis con MockLLM: parse falla → loguea y **reintenta** (no crashea,
      no escribe basura). Con Gemini real parsearía.

> Nota: `devicesim` debe usar un **device id que sea uuid y exista en `devices`**
> (FK NOT NULL). Si manda un id arbitrario, la persistencia se salta sin romper.

---

# P2 + P3 — HECHO ✅ (2026-06-14)

## P2 — Analysis worker (Gemini infiere de la conversación)
- [x] `internal/analysis/classifier.go` — `Classifier` sobre `LLMService`, prompt
      de clasificación → JSON `{cognitive_area, question_type, complexity,
      sentiment, topics[]}`. Parse robusto (limpia ```fences```), clamp 1-5.
- [x] `internal/analysis/classifier_test.go` — parse de JSON con fences + clamp.
- [x] `internal/analysis/worker.go` — poll `ListUnanalyzedInteractions` por ticker,
      clasifica y escribe `UpsertInteractionAnalysis` + por topic
      `UpsertTopic`/`LinkInteractionTopic`/`BumpChildTopicStat` en **una tx**
      (atómico; si falla se reintenta al próximo tick).
- [x] `cmd/mqtt/main.go` — arranca el worker (5s, batch 10) cuando hay DB.

## P3 — Dashboard read endpoints
- [x] `internal/api/dashboard_handlers.go` + rutas en `server.go`:
      `GET /api/children/{id}/{overview,usage,topics,areas,recommendations,interactions}`
      — todos con check de ownership (`ownChild`).
- [x] `internal/store/dashboard.go` (hand-written) — `ChildUsage` con bucket
      casteado a `timestamptz` (arregla el bug de `pgtype.Interval` que generó
      sqlc) + `CountInteractionsByChild`.
- [x] `go build` / `go vet` / `go test -race` limpios.

## Verificado contra Postgres real — HECHO ✅ (2026-06-14)
- [x] Seed manual de `interaction_analysis` + `topics` + `interaction_topics` +
      `child_topic_stats` (worker no corre con MockLLM).
- [x] Los 6 endpoints `GET /api/children/{id}/{overview,usage,topics,areas,
      recommendations,interactions}` devuelven JSON correcto con login real (JWT).
- [x] `GET /api/children/{id}/usage` valida el **fix del bucket**: devuelve
      `{"bucket":"2026-06-14T00:00:00-05:00","interaction_count":1}` sin error de
      scan (el `pgtype.Interval` que generó sqlc habría fallado).
- [x] Ownership: child ajeno → 404; sin auth → 401.
- [ ] sqlc NO está instalado → no se pudo regenerar; `ChildUsageByHour` generado
      sigue roto pero ya no se usa (lo reemplaza `ChildUsage`).

## Fuera de alcance hoy (no tocar)

- Análisis con Gemini / clasificación / topics / áreas cognitivas → **P2**.
- Campo "quién empezó la conversación" → innecesario (turno ya implica hablante).
- Comportamiento proactivo de Crowbot → **P5**.
- README en inglés y PR a `main` → después de cerrar esto.
