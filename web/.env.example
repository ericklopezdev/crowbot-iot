# API real del backend Go
VITE_API_URL=http://localhost:8080

# true  -> usa fixtures de src/mocks (ideal para capturas sin backend)
# false -> pega contra VITE_API_URL con axios + login real
VITE_USE_MOCKS=true
